import React from "react";

export default class Rank extends React.Component
{
    constructor(props)
    {
        super(props)
    }
    render()
    {
        return(
            <React.Fragment>
                in Rank
            </React.Fragment>
        );
    }
}